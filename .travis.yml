language: csharp
solution: DotNetStandardCalculator.sln

dotnet: 1.1.5
mono: none

install:
  - dotnet restore

stages:
  - test
  - name: package
    #if: branch = master

jobs:
  include:
    - stage: test
      script:
        - dotnet test ./DotNetStandardCalculatorTests/DotNetStandardCalculatorTests.csproj
    - stage: deploy
      script:
        - dotnet pack -c Release ./DotNetStandardCalculator/DotNetStandardCalculator.csproj
        - echo "To be implemented"